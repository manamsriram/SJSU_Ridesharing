apiVersion: v1
kind: ServiceAccount
metadata:
  name: booking-service-ksa
  namespace: lessgo
  annotations:
    iam.gke.io/gcp-service-account: booking-service-gsa@lessgo-492322.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: lessgo
  labels:
    app: booking-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
        tier: backend
    spec:
      serviceAccountName: booking-service-ksa
      enableServiceLinks: false
      containers:
      - name: booking-service
        image: us-west1-docker.pkg.dev/lessgo-492322/microservices-images/booking-service:${COMMIT_SHA}
        imagePullPolicy: Always
        ports:
        - containerPort: 3004
          name: http
        volumeMounts:
        - name: secrets-store
          mountPath: /mnt/secrets-store
          readOnly: true
        envFrom:
        - configMapRef:
            name: lessgo-config
        env:
        - name: STRIPE_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: lessgo-stripe-secret
              key: STRIPE_SECRET_KEY
        resources:
          # BENCHMARK: elevated for 20-VU load test. Restore to production values after testing:
          #   requests: cpu 50m, memory 64Mi  |  limits: cpu 200m, memory 128Mi
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: lessgo-secrets-provider
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: lessgo
  labels:
    app: booking-service
spec:
  selector:
    app: booking-service
  ports:
  - protocol: TCP
    port: 3004
    targetPort: 3004
    name: http
  type: ClusterIP
